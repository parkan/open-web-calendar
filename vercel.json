{
  "version": 2,
  "buildCommand": "cp requirements/base.txt requirements.txt && vercel build",
  "functions": {
    "./app.py": {
      "runtime": "python3.12",
      "maxDuration": 30
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ]
}
